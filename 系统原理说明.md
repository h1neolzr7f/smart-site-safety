# 智慧工地人员安全行为识别系统原理说明

## 1. 数据输入

系统使用 `data/HelmetHead` 作为训练与测试数据源。数据集按 `train`、`valid`、`test` 三个子集组织，每个子集包含 `images` 和 `labels`。

标签采用 YOLO 标注格式：

```text
class_id x_center y_center width height
```

当前类别：

```text
0: head      # 未佩戴安全帽
1: helmet    # 已佩戴安全帽
```

## 2. 模型训练

使用 `ultralytics.YOLO` 加载轻量检测模型，通过 `safety_dataset.yaml` 指向本地数据集，输入尺寸 640，混合精度训练，结果写入 `runs/detect`。

默认权重：

```text
models/best.pt
models/yolov26n-pose.pt
```

## 3. 检测流程

系统接收图片、摄像头帧或文件夹图片后，调用 YOLO 做单阶段检测，输出类别、置信度和边界框。

- `helmet`：已佩戴安全帽
- `head`：未佩戴安全帽，写入预警

## 4. 人体安全姿态检测

YOLO Pose 输出 17 个关键点。规则判定：

- 危险俯身 / 临边探空：肩髋中心连线相对竖直方向倾角超过阈值
- 疑似倒地 / 跌倒：躯干接近水平，且包围框宽高比偏大，或头脚垂直跨度明显变小

## 5. Web 前端

Streamlit 单页应用：

- 侧栏：功能导航、检测参数、姿态阈值、系统状态
- 顶部：安全帽状态、姿态状态、目标数、FPS、当前模型
- 主区：图片检测、摄像头实时监测、批量检测、模型上传、预警管理、结果展示与导出

## 6. 预警与导出

检测到未戴帽、危险俯身或疑似倒地时，保存截图并记录时间、级别、内容和处理状态。检测结果与预警日志可导出为 CSV 或 Excel。

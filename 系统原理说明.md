# 系统说明

## 数据

训练与测试数据位于 `data/HelmetHead`，划分为 `train`、`valid`、`test`，各含 `images` 与 `labels`。

标注格式：

```text
class_id x_center y_center width height
```

类别：

```text
0: head      # 未佩戴安全帽
1: helmet    # 已佩戴安全帽
```

## 训练

使用 `ultralytics.YOLO` 读取 `safety_dataset.yaml`，输入尺寸 640，混合精度训练，结果写入 `runs/detect`。

默认权重：

```text
models/best.pt
models/yolov26n-pose.pt
```

## 检测流程

输入单张图像、摄像头帧或文件夹后，YOLO 输出类别、置信度与边界框。

- `helmet`：已佩戴安全帽
- `head`：未佩戴安全帽，写入预警

## 姿态判定

YOLO Pose 输出 17 个关键点，判定规则如下：

- 肩髋中心连线相对竖直方向倾角超过阈值：危险俯身
- 躯干接近水平，或包围框宽高比偏大、头脚垂直跨度明显减小：疑似倒地

## 界面

Streamlit 单页应用：

- 侧栏：功能导航、检测参数、姿态阈值、运行状态
- 顶部：安全帽状态、姿态状态、目标数、FPS、当前模型
- 主区：图像检测、摄像头监测、批量检测、模型切换、预警管理与导出

## 预警与导出

检测到未戴帽、危险俯身或疑似倒地时保存截图，并记录时间、级别与处理状态。检测结果与预警日志可导出为 CSV 或 Excel。

# 系统怎么跑

## 数据

训练和测试图在 `data/HelmetHead`，分成 `train`、`valid`、`test`，下面各有 `images` 和 `labels`。

标签是 YOLO 那种：

```text
class_id x_center y_center width height
```

两类：

```text
0: head      # 没戴帽
1: helmet    # 戴了帽
```

## 训练

`ultralytics.YOLO` 读 `safety_dataset.yaml`，输入 640，混合精度，结果写到 `runs/detect`。

默认权重：

```text
models/best.pt
models/yolov26n-pose.pt
```

## 检测

丢一张图、一帧摄像头或一个文件夹进去，YOLO 给出类别、置信度和框。

- `helmet`：戴了
- `head`：没戴，记一条预警

## 姿态

Pose 出 17 个点。规则很简单：

- 肩髋中线歪得太厉害，算危险俯身
- 躯干快躺平，或者框又宽又扁、头脚距离变短，算疑似倒地

## 页面

Streamlit 一页：

- 左边：功能、阈值、状态
- 上面：戴帽情况、姿态、人数、FPS、当前模型
- 中间：图片、摄像头、批量、换模型、预警、导出

## 预警

没戴帽、弯太狠或像摔倒时，存一张截图，记下时间和处理状态。日志可以导出 CSV 或 Excel。

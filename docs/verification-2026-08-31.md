# 本地验证记录 — 2026-08-31

基础源码：`5bd79681b98643e81c3071ce4172de475dc46625`。

在隔离副本中运行，未修改原本地项目。环境为 Windows / Python 3.13、Streamlit 1.62.0、PyTorch 2.13.0 CPU、Ultralytics 8.4.136、OpenCV 5.0.0。依赖未锁定，本次没有验证所有版本组合。

## 执行内容

1. 按 README 安装依赖，以本地地址启动 Streamlit。
2. 使用 Streamlit AppTest 加载页面，选择 `helmet_gongdi_00240.jpg`，点击“开始图片检测”。页面异常列表为空，结果表为 2 行。
3. 在真实浏览器中重复相同步骤，保存 `screenshots/overview.png`。安全帽检测和姿态流程各返回一行；它们不代表两个独立人员。
4. `app.py` 通过 Python 语法检查。代码变化仅涉及样例与模型文件名显示，不改变推理路径、权重或阈值。

## 截图与数据

截图未经绘制或修改，来自应用的实际检测页面。样例位于 `data/HelmetHead/test/images/helmet_gongdi_00240.jpg`，权重来自本仓库的 `models/`。沿用已有样例不代表维护者拥有完整数据集的版权；重新分发或部署时应核对原数据和模型许可。

界面中的 FPS 来自单次处理耗时，不是持续视频或硬件性能基准。表格置信度也不能解释为整体模型准确率。

没有访问摄像头、重新训练、评估完整测试集、验证工业现场表现或测试导出与预警管理的所有路径。已有训练曲线及指标是历史产物，不是本轮新结果。
